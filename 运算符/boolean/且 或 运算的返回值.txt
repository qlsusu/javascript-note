---------------------------------&&的返回值
返回可能会导致false的值，如果没有，就返回最后一个值

true && 'foo' && '' && 4 && 'foo' && true
// ''
1 && 2 && 3
// 3

---------------------------------||的返回值
返回可能产生true的值，如果没有，就返回最后一个值
false || 0 || '' || 4 || 'foo' || true
// 4
false || 0 || ''
// ''
